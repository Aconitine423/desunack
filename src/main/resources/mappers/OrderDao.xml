<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >


<mapper namespace="com.desunack.desunack.dao.OrderDao">
    <insert id="makeOrder">
        insert into goods_order(go_receiver_name, go_m_uid, go_receiver_phone, go_receiver_post, go_receiver_address, go_receiver_address_detail, go_date, go_status, go_total_cost)
        values (#{mEntity.m_name}, #{mEntity.m_uid}, #{mEntity.m_phone}, #{mEntity.m_post}, #{mEntity.m_address}, #{mEntity.m_address_detail}, default, 0, #{total_cost})
    </insert>
    <insert id="insertOrderDetail">
        insert into go_detail(god_go_num, god_g_id, god_qty)
        values
        <foreach collection="scList" item="shoppingCart" separator=","> (#{god_go_num}, #{shoppingCart.sc_g_id}, #{shoppingCart.sc_qty})</foreach>
    </insert>
    <update id="updateOrder">
        update goods_order set go_receiver_name=#{go_receiver_name}, go_receiver_phone=#{go_receiver_phone}
        ,go_receiver_post = #{go_receiver_post}, go_receiver_address = #{go_receiver_address},
         go_receiver_address_detail = #{go_receiver_address_detail}, go_status = 1 where go_num = #{go_num}
    </update>
    <update id="updateGoods">
        update goods set g_qty = g_qty-#{sc_qty} where g_id = #{sc_g_id}
    </update>
    <delete id="deleteShoppingCart">
        delete from shopping_cart where sc_m_uid = #{uid} and (sc_g_id in
            <foreach collection="scList" item="shoppingCart" open="(" close=")" separator=",">#{shoppingCart.sc_g_id}
                                  </foreach>)
    </delete>

    <select id="getUserUID" resultType="java.lang.Integer">
        select distinct sc_m_uid from shopping_cart where sc_m_uid in
        <foreach collection="scList" item="sc_m_uid" open="(" close=")" separator=",">
            #{sc_m_uid}
        </foreach>

    </select>
    <select id="getGoodsInfo" resultType="java.lang.String">
        select json_object('g_image', g_image, 'g_name', g_name, 'g_value', g_value)
        from goods where g_id in
        <foreach collection="idList" item="id" open="(" close=")" separator=",">#{id}</foreach>
        order by g_id
    </select>

    <select id="getShoppingCartString" resultType="java.lang.String">
        select json_object('sc_g_id', sc_g_id, 'sc_qty', sc_qty) from shopping_cart
                where sc_m_uid = #{userUid} and (sc_g_id in
                <foreach collection="idList" item="id" open="(" close=")" separator=",">
                    #{id}
                </foreach> ) order by sc_g_id
    </select>
    <select id="getShoppingCart" resultType="com.desunack.desunack.dto.ShoppingCartDto">
        select * from shopping_cart where sc_m_uid = #{mUid} and (sc_g_id in <foreach collection="idList" item="id" open="(" close=")" )
    </select>
</mapper>